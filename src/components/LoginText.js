import { Text } from 'react-native';
import { estilos } from '../styles/StylesText';

export default function LoginText() {
    return (

        <Text style={estilos.loginText}>Insira Usuário e Senha</Text>
    )
}